const mysql = require('mysql');

class DataBase {

    constructor(host, user, password) {
        this.host = host;
        this.user = user;
        this.password = password;
    }

    createTable(tableName) {

    }
}